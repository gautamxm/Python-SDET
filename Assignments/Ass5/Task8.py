# Define a lambda function that takes two arguments, x and y, and
# returns their product. Call this lambda function with sample values.

product = lambda x,y: x*y
print(product(3,2))   # 6
print(product(5,2))   # 10
